<<: !include ../common/config.yaml

location:
  source_directories:
    - /mediaserver
  repositories:
    - /mnt/nas_dev_backup/mediaserver

storage:
  encryption_passcommand: cat ~/.config/secrets/mediaserver-borg-key